query getManagerProducts {
  allProducts {
    __typename
    ... on Product {
      name
      id
      actionsAllowed
    }
    ... on ProductInBackorder {
      name
      id
      actionsAllowed
    }
    ... on ProductInTransit{
      name
      id
      actionsAllowed
      customerId
    }
  }
}